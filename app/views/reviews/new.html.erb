<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::NUMB %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
<%= link_to 'Back', restaurant_path(@restaurant) %> |

